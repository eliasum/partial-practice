{
  "compilerOptions": {
    "target": "ES5",                           // Компиляция в ES5 для совместимости с старыми браузерами
    "noEmitOnError": true,                     // Не генерировать файлы .js, если есть ошибки компиляции
    "module": "CommonJS",                      // Использование CommonJS для Node.js (стандарт модулей для Node.js)
    "outDir": "./out",                         // Папка, куда будут выводиться скомпилированные файлы JavaScript
    "sourceMap": true,                         // Генерация карт исходных файлов для отладки (например, для просмотра исходного кода в браузере)
    "strict": true,                            // Включает все строгие проверки типов для улучшения качества кода
    "esModuleInterop": true,                   // Разрешает совместимость с CommonJS для импорта/экспорта в ES-модулях
    "skipLibCheck": true,                      // Пропуск проверки типов для библиотек (ускоряет компиляцию)
    "forceConsistentCasingInFileNames": true   // Согласованность регистров в названиях файлов (важно для работы на разных операционных системах)
  },
  "include": [
    "src/**/*.ts"                              // Включаем все файлы .ts в папке src и её подкаталогах
  ],
  "exclude": [
    "node_modules"                             // Исключаем папку node_modules из компиляции
  ]
}
